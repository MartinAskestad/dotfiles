" ## Basics
set nocompatible                                                                " Not compatible with VI
set showcmd
filetype plugin indent on                                                       " Add filetype, plugin, indent support
syntax on                                                                       " Turn on syntax highlighting

" ## Settings
set backspace=indent,eol,start                                                  " Allow to backspace everything in insert mode
set wildmenu                                                                    " Display matches in command-line mode
set expandtab                                                                   " Prefer tabs over spaces
set tabstop=2 softtabstop=2                                                     " Set tab size to 2
set shiftwidth=2
set smartindent
set tabstop=2 softtabstop=2                                                     " Set tab size to 2
set shiftwidth=2
set smartindent
set hidden                                                                      " Prefer hiding over unloading buffers
set path=.,**                                                                   " Relative to current file and everything under :pwd
setl wildignore=**/node_modules/**,**/dist/**
set noswapfile
let mapleader = " "                                                             " Space as <leader>
set encoding=utf-8
set fileencoding=utf-8
set ruler                                                                       " Show cursor position at all times
set cursorline                                                                  " Highlight current line
set cursorlineopt=line
set cmdheight=2                                                                 " more space for displaying messages
set splitbelow splitright
set t_Co=256
set laststatus=2                                                                " Always show status line
set showtabline=2                                                               " Always show tab line
set updatetime=300
set timeoutlen=1000
set clipboard=unnamed
set colorcolumn=80
set scrolloff=5
set pumheight=10
set nowrap
set switchbuf=usetab,newtab

" Change to project dir when opening certain files
let g:autochdir_project = 1


" ## Keyboard mappings
vnoremap . :        " Period is easier to reach on swedish keyboard
vnoremap : .
nnoremap . :
nnoremap : .

" Natural line jumping over wrapped lines
nnoremap j gj
nnoremap k gk
vnoremap j gj
vnoremap k gk

" Set up keybindings for asyncomplete
inoremap <expr> <Down> pumvisible() ? "\<C-n>" : "\<Down>"
inoremap <expr> <Up>   pumvisible() ? "\<C-p>" : "\<Up>"
inoremap <expr> <CR>   pumvisible() ? asyncomplete#close_popup() : "\<CR>"
inoremap <expr> <Tab>  pumvisible() ? asyncomplete#close_popup() : "\<Tab>"
inoremap <expr> <Left> pumvisible() ? asyncomplete#cancel_popup() : "\<Left>"

" Change window with <C-hjkl>
nnoremap <C-h> <C-w>h
nnoremap <C-j> <C-w>j
nnoremap <C-k> <C-w>k
nnoremap <C-l> <C-w>l

" Resize windows with <A-HJKL>
nnoremap <A-h> :vertical resize +2<CR>
nnoremap <A-j> :resize -2<CR>
nnoremap <A-k> :resize +2<CR>
nnoremap <A-l> :vertical resize -2<CR>

" Tabs
noremap <leader>th :tabprevious<CR>
noremap <leader>tj :tabmove -1<CR>
noremap <leader>tk :tabmove +1<CR>
noremap <leader>tl :tabnext<CR>
noremap <leader>tH :tabfirst<CR>
noremap <leader>tL :tablast<CR>
noremap <leader>to :tabnew<SPACE>

" Misc keymappings
nnoremap <leader>D :cd %:p:h<CR>                                                " Set working directory to current files dir
nnoremap <leader>? :echo expand('%:p')<CR>                                      " Print current file path

" Allow for alt-key in WSL/Windows terminal
let c='a'
while c <= 'z'
  exec "set <A-".c.">=\e".c
  exec "imap \e".c." <A-".c.">"
  let c = nr2char(1+char2nr(c))
endw
set ttimeout ttimeoutlen=50

" ## Plugins
colorscheme PaperColor

" Configure lightline
let g:lightline = {
  \ 'colorscheme': 'PaperColor',
  \ 'active': {
  \   'left': [ [ 'mode', 'paste' ],
  \             [ 'gitbranch', 'readonly', 'filename', 'modified' ] ]
  \   },
  \ 'component_function': { 'gitbranch': 'FugitiveHead' }
  \ } 

" Configure asyncomplete
let g:asyncomplete_auto_completeopt = 0
set completeopt=menuone,noinsert

" vim-signify
let g:signify_sign_show_count = 0
nmap <leader>gj <plug>(signify-next-hunk)
nmap <leader>gk <plug>(signify-prev-hunk)

" vim-vsnip
let g:vsnip_snippet_dir = expand('$HOME/.local/vim-vsnip')
imap <expr> <C-l> vsnip#available(1) ? '<Plug>(vsnip-expand-or-jump)' : '<C-l>'
smap <expr> <C-l> vsnip#available(1) ? '<Plug>(vsnip-expand-or-jump)' : '<C-l>'

augroup numbertoggle
	autocmd!
	autocmd BufEnter,FocusGained,InsertLeave,WinEnter *.ts,*.html,*.css,*.scss if &nu && mode() != "i" | set rnu    | endif
	autocmd BufLeave,FocusLost,InsertEnter,WinLeave   *.ts,*.html,*.css,*.scss if &nu                  | set nornu  | endif
augroup END
